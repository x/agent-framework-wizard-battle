[project]
name = "google-adk-wizard"
version = "0.1.0"
requires-python = "==3.13.12"
dependencies = [
    "chromadb>=1.5.1",
    "fastapi>=0.133.1",
    "google-adk>=1.0.0",
    "jinja2>=3.1.6",
    "litellm>=1.0.0",
    "python-dotenv>=1.0.0",
    "uvicorn>=0.41.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
    "ty>=0.0.18",
]

[tool.ruff]
line-length = 120
indent-width = 4

[tool.ruff.lint]
select = [
    "A",      # Prevent builtins shadowing
    "ANN",    # Require type annotations
    "ASYNC",  # Good async rules
    "B",      # Good common bug catching
    "C4",     # Catch incorrect use of comprehensions, dict, list, etc
    "C90",    # Reduce function complexity
    "D",      # Require docstrings
    "DTZ",    # No naive datetimes
    "E",      # PyCodeStyle errors
    "ERA",    # No commented out code
    "F",      # Pyflakes rules
    "FIX",    # FIXMEs and TODOs
    "FURB",   # Modern python guidance
    "I",      # Sort imports properly
    "ICN",    # Use common import conventions
    "ISC",    # Good use of string concatenation
    "LOG",    # Require cood logging
    "NPY",    # Some numpy-specific things
    "PLC",    # Good conventions, specifically PLC0415 for imports not in functions
    "PTH",    # Use pathlib instead of os.path
    "RET",    # Good return practices
    "SIM",    # Common simplification rules
    "T20",    # No print statements
    "TC",     # Enforce importing certain types in a TYPE_CHECKING block
    "TID",    # Some good import practices
    "TRY",    # Better exception handling
    "UP",     # Warn if certain things can changed due to newer Python versions
    "W",      # PyCodeStyle warnings
]

[tool.ruff.lint.per-file-ignores]
"models.py" = ["A003"]
"app.py" = [
    "C901",   # SSE generator and chat handler are inherently complex
    "E501",   # LLM instruction strings are intentionally long
    "TC002",  # ToolContext needed at runtime for ADK tool introspection
    "TC003",  # AsyncGenerator kept out of TYPE_CHECKING for simplicity
]
"tests/**/*.py" = ["D", "ANN", "C90", "T20", "LOG", "RET", "TRY", "FIX"]
"eval.py" = ["D", "ANN", "C90", "T20", "LOG", "RET", "TRY", "FIX", "SIM", "B905", "E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
